// function declaration and definition
// when you declaring a variable with an initial value, thats both declaring and defining the variable

let age = 25 // this is both declaring and defining the variable

let name1 // declaring a variable without a value (not defined yet)

name1 = "Mahi" // now defining the variable with a value


// function declaration (not defined yet)
function printMe(){
    console.log("Printing me....")
}

// calling the function (definition already there in function declaration)
printMe() // calling or invoking  the function


let mark; // declared the mark variable

mark = 56  // defined the variable


function markShow(){
    return mark
}

console.log(markShow())
